package test

import "testing"

func TestSetNext(t *testing.T) {
    // Given
    //    system := initSystem()

    // When
    //    user.Follow(user1)

    // Then
    //    assert.Equal(t, 1, len(user.GetFollowings()))
}

func TestHandle(t *testing.T) {
    // Given
    //    system := initSystem()

    // When
    //    user.Follow(user1)

    // Then
    //    assert.Equal(t, 1, len(user.GetFollowings()))
}
